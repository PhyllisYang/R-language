setwd('/Users/fanyang/EE627_HW/HW/HW1')
M<-read.csv('EE627A_HW1_Data.csv',header=TRUE)
head(M)
theMatrix<-as.matrix(M)
head.matrix(theMatrix)
Mkt<-seq.default()
SMB<-seq.default()
HML<-seq.default()
RF<-seq.default()
Mom<-seq.default()
for(i in 7:36){
  Mkt[i-6]<-cor(theMatrix[,2],theMatrix[,i])
  SMB[i-6]<-cor(theMatrix[,3],theMatrix[,i])
  HML[i-6]<-cor(theMatrix[,4],theMatrix[,i])
  RF[i-6]<-cor(theMatrix[,5],theMatrix[,i])
  Mom[i-6]<-cor(theMatrix[,6],theMatrix[,i])
}
Mkt<-as.matrix(Mkt)
SMB<-as.matrix(SMB)
HML<-as.matrix(HML)
RF<-as.matrix(RF)
Mom<-as.matrix(Mom)
whole<-cbind(Mkt,SMB,HML,RF,Mom)
whole<-as.matrix(whole)
nrow(whole)
rownames(whole)<-colnames(M[,7:36])
colnames(whole)<-colnames(M[,2:6])
head.matrix(whole)
write.csv(whole,'HW1_part1.csv')
